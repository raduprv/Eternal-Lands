Cicero:
Add music (Ogg vorbis player) -- testing
log, cfg, & ini in user directory tree
	-Specify alternate directory to load games files from
S/W version check & upload -- testing
	-Check for newer versions of files by comparing MD5 sums
	-http download of a file
User defined keyboard/mouse bindings

Learner:
OSX support - delayed :(
Faster mouse icon switch without dropping FPS
Investigate variable width fonts - preliminary testing
save zoom level and angle in el.cfg
Better faq support
Change the internal way MD2s are loaded, and make them vertex arrays. - TESTING


Ent's original list:
Depth shadows (very complicated) 
Various spell effects. (medium-hard) 
Buddy list menu (will do the server part myself) 
On card that support OVB, make the 3d objects and models VBO.
Make use of other extensions, if supported (such as texture compression, bump mapping, etc.) 


Unassigned:
Chat Window
MultiChannel Chat (needs server support)
Server selection screen (we only have one server right now...)
rework internal windowing system to keep track of which window is on top and
	to draw them and catch clicks accordingly

